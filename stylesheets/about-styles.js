import {
  StyleSheet,
  Dimensions,
} from 'react-native';

var {screenH, screenW} = Dimensions.get('window');

const styles = StyleSheet.create({
  scrollContainer: {
    margin: 15,
  }
});

export {styles};